# Avaliacao

Os casos em `test_cases.yaml` definem o comportamento minimo esperado do agente: respostas fundamentadas, recusa quando nao ha evidencias e questoes com explicacao.

As avaliacoes devem medir factualidade, cobertura das fontes, qualidade pedagogica, citacoes e latencia. Nunca use a chave Gemini em fixtures ou logs.
